import { Route, Routes } from 'react-router-dom';
import './index.scss';

export const App = () => {
  return (
    <div className="app">
      <Routes>
        <Route path="/"/>
      </Routes>
    </div>
  )
}